import 'package:flutter/material.dart';

class TodoScreen extends StatelessWidget {
  const TodoScreen({Key? key}) : super(key: key);
  static String routeName = '/todo_screen' ;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
