import 'package:flutter/material.dart';

class ToDoListScreen extends StatelessWidget {
  const ToDoListScreen({Key? key}) : super(key: key);
  static String routeName = '/todo_list_screen' ;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
